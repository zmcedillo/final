import { CacheFileData, ScannerProperties } from './types';
export declare function getCacheFileLocation(properties: ScannerProperties, { checksum, filename, alias }: CacheFileData): Promise<string | null>;
export declare function extractArchive(fromPath: string, toPath: string): Promise<void>;
export declare function validateChecksum(filePath: string, expectedChecksum: string): Promise<void>;
export declare function getCacheDirectories(properties: ScannerProperties, { checksum, filename }: CacheFileData): Promise<{
    archivePath: string;
    unarchivePath: string;
}>;
